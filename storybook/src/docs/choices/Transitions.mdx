import { Meta, Markdown } from '@storybook/blocks';

import { TokenGroup } from '@/storybook-helpers';

<Meta title="foundation/Choices/Transitions" />

# Transitions

Click on the Preview to see the TransitionEvent.
<TokenGroup path="choiceGroupsByTheme.default.transitions" visualizer="transition"/>
