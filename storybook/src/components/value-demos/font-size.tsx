export const FontSizeDemo = ({ value }: { value: string }) => {
  return (
    <div style={{ display: 'flex', height: '48px' }}>
      <div style={{ fontSize: value, marginTop: 'auto', marginBottom: 'auto' }}>
        Aa
      </div>
    </div>
  );
};
