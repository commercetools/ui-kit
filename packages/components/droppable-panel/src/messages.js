import { defineMessages } from 'react-intl';

export default defineMessages({
  useInputSearch: {
    id: 'ColumnManager.DroppablePanel.useInputSearch',
    description:
      'A message to indicate to the user that he can load more attributes by using the search input',
    defaultMessage: 'Load more attributes by using the search input above',
  },
});
