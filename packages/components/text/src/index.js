import { Headline, Subheadline, Body, Detail, Wrap } from './text';

const Text = {
  Headline,
  Subheadline,
  Body,
  Detail,
  Wrap,
};
export default Text;
export { default as version } from './version';
